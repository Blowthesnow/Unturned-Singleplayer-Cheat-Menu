# v1.6.0 验收矩阵

状态说明：

- **源码确认**：从当前源代码或程序集签名验证。
- **构建确认**：Release 构建或自动化冒烟测试验证。
- **运行确认**：在无 BattlEye 的真实 Unturned 单人世界中验证。

| 范围 | 验证内容 | 结果 |
| --- | --- | --- |
| 快捷键 | 默认 `End`，可通过 BepInEx 配置修改；Unity 与原生输入在同次按键相邻帧上报时只接受一次 | 源码、烟囱测试；默认快捷键既有运行确认 |
| 单人限制 | 检查 connected/server/client、`Singleplayer_` server ID 和本地玩家；条件失效时自动关闭 | 源码、构建、运行确认 |
| 菜单与输入 | 六个主标签可切换；菜单打开时显示原生光标并隔离角色输入，关闭后恢复 | 运行确认 |
| 界面本地化 | 活动 Overlay 的页签、控件、分类、状态消息和空状态支持中文与英文；资产名、地图名和用户文本保持原样 | 源码、构建、运行确认 |
| 语言切换 | 窗口右上角 `EN` / `中文` 按钮即时重建当前页并写回 `Interface.Language`，无需重启 | 构建、运行确认 |
| 角色 | 无敌、无限生存、治疗、指定生存值、经验、声望、技能满级 | 源码、构建、运行确认 |
| 物品扫描 | `Assets.find(List<ItemAsset>)` 枚举当前已加载原版、地图和 Workshop 资产 | 运行确认：扫描 5,084 项，其中模组 3,913 项 |
| 物品交互 | 主分类、精确类型、来源、稀有度、装备槽位、枪械机制、多选射击模式、搜索、分页、名称、来源、真实图标和数量 `1–255` | 高级筛选为源码、烟囱测试确认；基础给予与数量为既有运行确认 |
| 车辆扫描 | `Assets.find(List<VehicleAsset>)` 枚举当前已加载资产 | 运行确认：扫描 182 辆，其中模组 119 辆 |
| 车辆交互 | 分类、搜索、分页、名称、来源、官方图标或模型缩略图、批量 `1–20` | 运行确认：模组车辆生成在玩家前方 |
| 车辆缩略图设置 | `128 × 96`、`192 × 144`、`256 × 192` 三档尺寸，自动取景倍率 `0.5–1.5`，配置规范化与持久化 | 源码、烟囱测试、用户倍率截图和新 DLL 加载日志确认 |
| 车辆缩略图缓存 | 车辆 GUID + 缓存格式 + 尺寸 + 取景倍率的 PNG 缓存；损坏缓存删除并回退渲染；重新扫描只清内存 | 源码、构建确认；磁盘命中性能边界未重新做完整实机计时 |
| 收藏 | 星标新增/取消、GUID 持久化、空集合保存、重启恢复 | 构建和运行确认 |
| 收藏页 | 物品/车辆切换、分类、搜索、分页、数量以及直接给予/生成 | 运行确认 |
| 传送 | 保存多个具名位置、地图隔离、传送、删除、重启恢复 | 构建和运行确认 |
| 地图传送 | 地图/列表双视图、缩放拖动、玩家位置、点击地图安全落点和建筑物顶部落点 | 源码、构建、用户截图确认；完整交互边界见 UI 回审文档 |
| 传送标记 | 星形/正方形/圆形/菱形、预设与自定义颜色、旧数据规范化 | 源码、构建确认 |
| 界面状态 | 保存并恢复最后主页面和传送子视图 | 源码、构建确认 |
| 时间与事件 | 时间滑条、白天、夜晚、冻结时间、满月、空投 | 运行确认 |
| 天气 | 雨、雪、清除天气、关闭天气调度 | 运行确认 |
| 地图变化 | 跨场景保留 Overlay 时更新当前地图标题 | 运行确认 |
| BepInEx/Harmony | BepInEx 5.4.23.5 加载插件；Harmony 补丁绑定当前 `PlayerLife.doDamage` | 运行日志确认 |
| 原生作弊开关 | 插件不读取 `Provider.hasCheats`，地图的“单人作弊指令”可保持关闭 | 源码和程序集确认 |
| 安全边界 | 测试使用无 BattlEye 启动；插件拒绝非单人世界 | 运行确认 |

## 最终构建

- 插件版本：`1.6.0`
- Release：`0` 警告、`0` 错误
- DLL 大小：`193,024` 字节
- DLL 文件版本 / 产品版本：`1.6.0.0` / `1.6.0`
- DLL SHA-256：`91270BD0E4B180146CCF472848A49297DAB9E4A3F1B04EC405A17091231A68ED`
- Plugin-Only ZIP SHA-256：`5198062C954864918D90614675461CFBE2C36106E15F99A17B8EB0A0B737EC6A`
- Full BepInEx 5 x64 ZIP SHA-256：`10659F9AFC946A553F5A8FA9B59D3C13516A893D9B27F9FF73AF4479865F4150`
- 本地化：语言解析、174 个活动 Overlay 静态模板、动态状态模板、用户文本保留和默认传送点名称测试通过
- 收藏 JSON：单点组合、完整字段和空集合显式数组往返通过
- 传送 JSON：单点完整字段和空集合显式数组往返通过
- 物品筛选：分类组合、来源、稀有度、槽位、枪械机制、射击模式 OR 语义、搜索和分类切换规范化通过
- 快捷键去重：同帧、相邻帧重复输入与运行时计时重置边界通过
- 车辆缩略图设置：尺寸、4:3 高度、倍率默认值/边界/异常值和稳定缓存键通过

此文档记录的是 v1.6.0 的源码、构建、烟囱测试与发布包证据，并明确继承哪些 v1.5.0 真实单人运行证据。项目当前 README 展示截图为 1.1 版；2.0 版将统一更新截图。本文不保证未来 Unturned 更新后内部 API 仍保持兼容。
